// Feel free to modify this!

// Vote model
// userId: string
// submissionId: string
// upVote: boolean

// key value
// votes
// [votes]

type Vote = {
  userId: string;
  submissionId: string;
  upVote: boolean;
};

export type O = {
  vArray: Vote[];
};
